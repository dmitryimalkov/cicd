# Test Stand — CI/CD + автоматизация архитектуры

## Что мы строим

Этот репозиторий — минимальный тестовый стенд для отработки двух связанных
автоматизаций:

1. **Автодеплой по push.** При изменении `docker-compose.yml` в ветке `main`
   GitHub Actions по SSH заходит на сервер, забирает изменения и
   перезапускает контейнеры (`docker compose up -d`).

2. **Автосинхронизация архитектурной модели.** При изменении
   `docker-compose.yml` или `workspace.dsl` отдельный job:
   - сравнивает список сервисов из `docker-compose.yml` со списком
     контейнеров в `workspace.dsl` (C4-модель в формате Structurizr DSL);
   - если появился новый сервис, которого нет в модели — добавляет для
     него заготовку блока `container` с пометкой `TODO` (связи между
     контейнерами скрипт не угадывает, это остаётся за человеком);
   - рендерит `workspace.dsl` в PlantUML, а затем в PNG-диаграммы
     (`SystemContext.png`, `Containers.png`);
   - коммитит обновлённые `workspace.dsl` и диаграммы обратно в
     репозиторий.

Оба job'а запускаются от одного и того же push и работают независимо друг
от друга. Оба используют прямой `git push` в `main` — предполагается, что
branch protection на ветке `main` не включена (если включите позже,
job с архитектурой нужно будет переделать под создание Pull Request).

## Структура репозитория

```
test-stand/
├── docker-compose.yml          # тестовый стек: nginx, app, postgres, redis
├── workspace.dsl               # C4-модель (Structurizr DSL)
├── architecture/
│   └── diagrams/                # сюда рендерятся .puml и .png
├── scripts/
│   ├── sync_dsl_with_compose.py # drift-check + автодополнение workspace.dsl
│   └── render_architecture.sh   # рендер workspace.dsl → PlantUML → PNG
└── .github/workflows/
    └── pipeline.yml              # deploy + architecture-sync
```

## Как настроить secrets в GitHub

Секреты нужны только для job `deploy` — он подключается к серверу по SSH.
Job `architecture-sync` секретов не требует (работает полностью на
GitHub-раннере, коммитит через встроенный `GITHUB_TOKEN`, который GitHub
подставляет автоматически).

1. Создайте отдельную SSH-пару **только для CI**, не используйте свой
   личный ключ:
   ```bash
   ssh-keygen -t ed25519 -f ./deploy_key -N "" -C "github-actions-deploy"
   ```
   Это создаст `deploy_key` (приватный) и `deploy_key.pub` (публичный).

2. Публичный ключ добавьте на сервер в `~/.ssh/authorized_keys` того
   пользователя, под которым будет выполняться деплой:
   ```bash
   ssh-copy-id -i deploy_key.pub deploy-user@ВАШ_СЕРВЕР
   ```
   (или вручную допишите содержимое `deploy_key.pub` в
   `/home/deploy-user/.ssh/authorized_keys` на сервере).

3. В репозитории на GitHub откройте
   **Settings → Secrets and variables → Actions → New repository secret**
   и добавьте четыре секрета:

   | Имя секрета | Значение |
   |---|---|
   | `SSH_HOST` | IP-адрес или домен вашей ВМ на cloud.ru |
   | `SSH_USER` | имя пользователя для деплоя (не root) |
   | `SSH_KEY` | содержимое приватного ключа `deploy_key` целиком, включая строки `-----BEGIN...` / `-----END...` |
   | `SSH_PORT` | порт SSH (обычно `22`, если не меняли) |

4. Удалите локальные файлы `deploy_key` / `deploy_key.pub` после того,
   как приватный ключ сохранён в GitHub Secrets — держать его на диске
   без необходимости не стоит.

5. Проверить, что секреты подставляются корректно, можно только через
   реальный запуск workflow (GitHub никогда не показывает значения
   секретов в логах, даже вам). Если `deploy` job падает на шаге
   `Deploy via SSH` с `Permission denied (publickey)` — почти всегда
   это означает, что публичный ключ не добавлен на сервере либо
   `SSH_USER`/`SSH_HOST` указаны неверно.

## Как проверить, что structurizr-cli корректно экспортирует workspace.dsl

Перед тем как полагаться на CI, стоит прогнать те же шаги локально —
на своей машине или прямо на тестовой ВМ на cloud.ru (потребуется только
Docker, ничего больше устанавливать не нужно).

1. **Проверить синтаксис DSL** (быстрая проверка без рендера):
   ```bash
   docker run --rm \
     -v "$(pwd):/usr/local/structurizr" \
     structurizr/cli validate -workspace workspace.dsl
   ```
   Если DSL некорректен, `structurizr-cli` выведет конкретную строку
   и описание ошибки (например, незакрытую скобку или ссылку на
   несуществующий элемент в `Rel`).

2. **Экспортировать в PlantUML и посмотреть на сырой вывод:**
   ```bash
   mkdir -p architecture/diagrams
   docker run --rm \
     -v "$(pwd):/usr/local/structurizr" \
     structurizr/cli export -workspace workspace.dsl -format plantuml -output architecture/diagrams

   cat architecture/diagrams/*.puml
   ```
   Если экспорт прошёл успешно — в папке появятся файлы `SystemContext.puml`
   и `Containers.puml` с текстовым описанием диаграмм на языке PlantUML.
   Если в выводе пусто или файлов нет — проверьте, что в `workspace.dsl`
   действительно есть блок `views` с `systemContext` и `container` view
   (без них экспортировать нечего).

3. **Отрендерить PlantUML в PNG и открыть глазами:**
   ```bash
   docker run --rm \
     -v "$(pwd)/architecture/diagrams:/data" \
     plantuml/plantuml -tpng "/data/*.puml"
   ```
   Откройте `architecture/diagrams/Containers.png` — на схеме должны быть
   все контейнеры из `workspace.dsl` со стрелками связей и подписями
   (не просто "depends on", а те смысловые описания, что вы указали в
   `Rel(...)`).

4. **Прогнать весь пайплайн одной командой** (то же самое, что выполнит
   CI):
   ```bash
   bash scripts/render_architecture.sh
   ```

5. **Проверить drift-check отдельно**, не трогая рендер — полезно, если
   просто хочется убедиться, что все сервисы из `docker-compose.yml`
   учтены в модели:
   ```bash
   pip install pyyaml
   python scripts/sync_dsl_with_compose.py
   ```
   Скрипт выведет `::warning::`, если найдёт сервис без модели, и сам
   допишет для него заготовку в `workspace.dsl` — после этого стоит
   открыть файл и убедиться, что заготовка добавилась в нужное место
   (внутри блока `softwareSystem { ... }`), а не за его пределами.

6. **Частая проблема на этапе тестирования на cloud.ru** — недоступность
   Docker Hub из региона или медленная скачка образов `structurizr/cli` и
   `plantuml/plantuml`. Проверьте это отдельно до встраивания в CI:
   ```bash
   docker pull structurizr/cli
   docker pull plantuml/plantuml
   ```
   Если `pull` зависает или падает — рендер лучше оставить только на
   стороне GitHub-раннера (`ubuntu-latest`), у которого с Docker Hub
   проблем нет, а на ВМ Docker вообще не использовать для этой задачи —
   готовые PNG туда попадут через `git pull`, как обычные файлы.
## Справочник имён сервисов

Автоматизация архитектуры (`architecture-sync`) сравнивает сервисы из
`docker-compose.yml` с контейнерами в `workspace.dsl` **чисто по
именам** — без какой-либо смысловой привязки. Имя сервиса в
`docker-compose.yml` (ключ в `services:`) должно **дословно совпадать**
с именем переменной-контейнера в `workspace.dsl` (`имя = container
...`). Регистр важен, имя образа (`image:`) в сравнении не участвует.

Если имена разойдутся — автоматика посчитает это двумя разными
сущностями: старое имя будет удалено из модели вместе со всеми
связями, а новое добавлено как "с нуля", с потерей вручную прописанных
`Rel`. Поэтому любое переименование сервиса в compose должно
сопровождаться таким же переименованием переменной и текста в
`workspace.dsl` в том же коммите.

Ниже — текущий справочник имён, назначения и технологии для всех
сервисов проекта. Обновляйте эту таблицу при каждом добавлении,
удалении или переименовании сервиса — она должна оставаться
источником правды наравне с самим `workspace.dsl`.

| Имя сервиса (= имя в compose и в DSL) | Назначение | Технология |
|---|---|---|
| `nginx` | Reverse proxy, точка входа снаружи | nginx |
| `app` | Backend-приложение, отвечает на HTTP-запросы | Python / FastAPI |
| `postgres` | Основная реляционная БД | PostgreSQL |
| `redis` | Кэш и очереди задач | Redis |
| `worker` | Фоновый обработчик задач из очереди Redis | Python |

### Соглашение об именовании

- Только строчные буквы и дефисы (`worker-cache`, а не `Worker_Cache`).
- Имя отражает роль сервиса в системе, а не технологию (`worker`, а не
  `python-worker`) — технология уже указывается отдельным полем в
  `container "..." "..." "технология"`.
- Не переиспользовать имя, которое раньше принадлежало другому
  сервису (даже если старый давно удалён) — это может запутать
  историю коммитов в `workspace.dsl`.

### Порядок действий при изменениях

- **Новый сервис**: добавить в `docker-compose.yml`, запушить —
  `architecture-sync` сам добавит заготовку `container` с `TODO` в
  `workspace.dsl`. Затем вручную: описать назначение и технологию
  вместо `TODO`, дописать связи (`Rel`) **после** всех объявлений
  `container`, и добавить строку в таблицу выше.
- **Удаление сервиса**: убрать из `docker-compose.yml` — при
  следующем пуше `architecture-sync` сам удалит контейнер и все его
  связи из `workspace.dsl`. Дополнительно нужно вручную убрать строку
  из таблицы выше (автоматика её не трогает) и, если сервис уже
  запущен на сервере, выполнить деплой с флагом `--remove-orphans`,
  чтобы убрать осиротевший контейнер:
  ```bash
  docker compose up -d --remove-orphans
  ```
- **Переименование сервиса**: переименовать ключ в
  `docker-compose.yml` **и** переменную вместе с текстом в кавычках в
  `workspace.dsl` в одном и том же коммите — иначе автоматика решит,
  что старый сервис удалён, а новый создан с нуля.

### Возможные будущие сервисы (справочник имён "на вырост")

Ниже — сервисы, которые могут появиться в проекте по мере роста
(например, при переходе части инфраструктуры на управляемые сервисы
cloud.ru вместо self-hosted контейнеров), с предложенными именами.
Использовать таблицу нужно так: **прежде чем придумывать имя новому
сервису**, сначала проверить, нет ли для него уже готового варианта
здесь — это предотвращает ситуацию, когда два разработчика называют
один и тот же логический компонент по-разному.

Источник — официальный список сервисов Cloud.ru Evolution
(https://cloud.ru/docs/evolution/overview/topics/services__list).
Не все сервисы облака имеют смысл как "контейнер" в архитектуре
(инфраструктурные вещи вроде виртуальных машин или дисков в C4-модели
обычно не описываются отдельным контейнером) — ниже отобраны только
те, что типично появляются как логический компонент системы.

| Предлагаемое имя сервиса | Назначение | Технология / сервис cloud.ru |
|---|---|---|
| `postgres` | Основная реляционная БД (уже используется) | Managed PostgreSQL |
| `redis` | Кэш и очереди задач (уже используется) | Managed Redis |
| `clickhouse` | Аналитическая БД для больших объёмов данных, логов, метрик | Managed ClickHouse |
| `opensearch` | Полнотекстовый поиск и агрегация логов | Managed OpenSearch |
| `kafka` | Брокер сообщений для событийной архитектуры между сервисами | Managed Kafka |
| `event-bridge` | Интеграционная шина (ESB) для приёма и маршрутизации событий | Event Bridge |
| `api-gateway` | Публикация, мониторинг и защита внешних API | API Gateway |
| `load-balancer` | Балансировка входящего трафика между репликами сервисов | Load Balancer |
| `artifact-registry` | Хранение Docker-образов и OCI-артефактов | Artifact Registry |
| `airflow` | Оркестрация фоновых рабочих процессов и ETL-пайплайнов | Managed Airflow |
| `spark` | Распределённая пакетная обработка больших данных | Managed Spark |
| `flink` | Обработка потоковых данных в реальном времени | Managed Flink (Preview) |
| `trino` | SQL-запросы поверх нескольких источников данных одновременно | Managed Trino |
| `metastore` | Хранение метаданных таблиц для аналитических сервисов | Managed Metastore |
| `arenadata-db` | СУБД на основе Greenplum для больших объёмов аналитики | Managed ArenadataDB |
| `bi` | Визуализация данных и дашборды | Managed BI |
| `rag` | Retrieval-Augmented Generation поверх собственных данных | Managed RAG |
| `ml-inference` | Инференс ML-моделей в контейнере | ML Inference |
| `ai-agents` | Мультиагентные AI-системы, встроенные в бизнес-процессы | AI Agents |
| `notebooks` | Интерактивная разработка и эксперименты с ML-моделями | Notebooks |
| `object-storage` | Хранение файлов, бэкапов, архивов, логов (S3-совместимое) | Object Storage |
| `dns` | Управление публичными/приватными доменами облака | Evolution DNS |
| `vpn-gateway` | VPN-туннели для защищённого доступа к внутренним ресурсам | Evolution VPN |

**Важно:** это справочник **предполагаемых** имён на будущее, а не
список того, что уже развёрнуто в текущем `docker-compose.yml` —
переносите строку из этой таблицы в основной справочник выше только
в момент, когда сервис реально появляется в проекте, и следите, чтобы
имя в `docker-compose.yml` и `workspace.dsl` совпадало с тем, что
здесь предложено (либо явно измените имя сразу в обеих таблицах,
если предложенный вариант не подошёл).

## Как придумывать новые имена, если справочника не хватило

Если нужного сервиса нет ни в основном справочнике, ни в таблице
"на вырост" — не изобретайте имя на ходу и не смотрите только на то,
как удобнее назвать прямо сейчас. Сначала проверьте оба справочника
ещё раз (в том числе — не подходит ли по смыслу более общее имя из
таблицы "на вырост", даже если планируемая технология немного
отличается от указанной там). Только если действительно ничего не
подошло — придумывайте новое имя по следующим правилам.

### Правила составления имени

1. **Только строчные латинские буквы, цифры и дефисы.** Никаких
   подчёркиваний, заглавных букв, кириллицы:
   `payment-gateway`, а не `Payment_Gateway` или `оплата`.
2. **Имя описывает роль сервиса в системе, а не технологию.**
   `notifications`, а не `rabbitmq` — технология и так указывается
   отдельным полем в `container "..." "..." "технология"`, и если
   технологию позже поменяют (например, RabbitMQ на Kafka), не
   придётся переименовывать сам сервис.
3. **Имя короткое и однозначное** — 1-2 слова через дефис, без
   аббревиатур, понятных только автору. `billing`, а не `bl-svc-v2`.
4. **Не переиспользовать имя, которое раньше принадлежало другому,
   уже удалённому сервису** — даже если кажется удобным. Проверьте
   историю `workspace.dsl` в git-логе, если сомневаетесь, использовалось
   ли имя раньше.
5. **Единственное число**, если сервис один в системе (`worker`, а не
   `workers`), и без указания окружения или версии в имени (`app`, а
   не `app-prod` или `app-v2`) — окружение и версия не относятся к
   архитектуре сервиса.

### Алгоритм действий при введении нового имени

1. Убедиться, что имя действительно новое (не совпадает ни с одной
   строкой в обеих таблицах справочника выше) и соответствует
   правилам составления из этого раздела.
2. Добавить сервис в `docker-compose.yml` под этим именем.
3. Запушить — `architecture-sync` сам создаст заготовку `container` с
   `TODO` в `workspace.dsl` под тем же именем (см. "Порядок действий
   при изменениях" выше).
4. Вручную дополнить заготовку: описание вместо `TODO`, технологию,
   связи (`Rel`).
5. **Обязательно** добавить новую строку в таблицу "Справочник имён
   сервисов" (основную, не "на вырост") этого README — с тем же
   именем, назначением и технологией, что и в `workspace.dsl`. Это
   отдельный, ручной шаг: автоматика синхронизирует только
   `docker-compose.yml` и `workspace.dsl`, а таблицу в README не
   трогает.
6. Если сервис задумывался как что-то более общее и может
   пригодиться в других проектах — рассмотреть, стоит ли также
   предложить его для добавления в общий (не привязанный к одному
   репозиторию) справочник команды, если такой ведётся отдельно от
   этого README.

Пропуск шага 5 — самая частая причина, по которой справочник
расходится с реальностью: код и модель архитектуры остаются
синхронизированными автоматически, а вот человекочитаемая таблица в
README — нет, и её никто, кроме самого разработчика, не обновит.
